# ArcIris

This repository is for training iris recognition models with ArcFace loss. Training is setup for the CRC.



## Setup

1. Activate a new venv with requirements:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

1. 
